spring:
  cloud:
    config:
      name: springcloud_basics   # github上的配置名称，注意没有yml后缀名
      profile: dev  # 默认使用配置版本
      label: master # 远程库的分支名
      uri: http://localhost:5001  # config 配置中心地址，通过它获取 microservice-config-application.yml 配置信息
